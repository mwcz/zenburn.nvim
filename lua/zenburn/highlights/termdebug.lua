local colors = require("zenburn.colors")
return {
	debugBreakpoint = { fg = colors.dark_sand, bg = colors.salmon500 },
	debugPC = { bg = colors.orange500, fg = colors.black200 },
	Debug = { fg = colors.salmon500 },
}
